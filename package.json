{
	"name": "file-management-system",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"start": "conc \"docker compose up\" \"npm run dev --prefix ./client\"",
		"install-all": "npm i && npm i --prefix ./client && docker compose build server"
	},
	"author": "zhon12345",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.0.1"
	}
}
