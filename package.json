{
	"name": "file-management-system",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"start": "conc \"npm run dev --prefix ./server\" \"npm run dev --prefix ./client\"",
		"install-all": "npm i && npm i --prefix ./server && npm i --prefix ./client"
	},
	"author": "zhon12345",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.0.1"
	}
}
